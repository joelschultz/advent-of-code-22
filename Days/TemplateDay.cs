namespace Days {
    public class TemplateDay : DaySolverBase {
        public override string Example1 =>
            @"";

        string Transform(string raw) {
            return raw;
        }

        public override object Solve1(string raw) {
            var input = Transform(raw);

            return -1;
        }

        public override object Solve2(string raw) {
            var input = Transform(raw);

            return -1;
        }
    }
}